{
  "name": "@raydeck/event-manager",
  "version": "1.0.2",
  "description": "Base class for managing events",
  "main": "lib/index.js",
  "types": "lib/types",
  "repository": "https://github.com/rhdeck/event-manager",
  "author": "Ray Deck",
  "license": "MIT",
  "scripts": {
    "test": "tsc --noEmit",
    "build": "tsc && typedoc --readme stub.md --mode file --excludeNotExported --includeVersion && cp docs/README.md . && git commit docs README.md -m \"docs: updating docs\" && git push",
    "watch": "livelink watch",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "typedoc": "^0.17.8",
    "typedoc-plugin-markdown": "^2.3.1",
    "typescript": "^3.9.7"
  }
}
